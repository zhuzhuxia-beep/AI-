# 生活娱乐赛道 · AI 老照片时光机

---

## 一、Demo 简介

**AI 老照片时光机** 是一个 Web 应用：用户上传一张老照片，AI 自动修复划痕、降噪、上色，然后用户讲述照片背后的故事，AI 生成中文配音，最终合成一段带有 Ken Burns 动态效果、背景音乐和字幕的回忆短片。

**面向谁：** 25-45 岁的子女——翻到父母或祖父母的旧照片，想给长辈一个惊喜；以及 50-70 岁的中老年人，怀念年轻时的照片，想重温青春记忆。

**核心功能：**

1. **自适应修复引擎** — 先评估照片质量（模糊度、噪点、是否灰度），再决定处理方式。高噪点照片用双边滤波 + NLMeans 双重降噪，模糊照片用 unsharp mask + Laplacian 核锐化，已经清晰的照片只做色彩增强。最后有质量守卫环节：对比修复前后的 Laplacian 方差，如果变糊了自动混合回原图。
2. **故事叙事引擎** — 用户写下照片背后的故事（提供 4 种场景模板快速填充），Edge TTS 生成中文配音，情感分析匹配氛围背景音乐（暖色记忆用 C 大调，怀旧伤感用 A 小调），FFmpeg 混合人声和背景音乐。
3. **Ken Burns 动态短片** — 照片不是静态展示，而是用 FFmpeg zoompan 滤镜实现缓慢缩放 + 平移，让照片"活"过来。配合配音、背景音乐、字幕烧录，合成 1080p 回忆短片。

**产品截图：**

> 📌 **[此处插入截图 1：首页全貌]** — 深色主题首页，橙色渐变标题，包含功能介绍、数据统计动画、作品画廊

> 📌 **[此处插入截图 2：上传 + 修复对比页]** — 拖拽上传区域 + AI 修复多步骤进度 + 修复前后对比滑块

> 📌 **[此处插入截图 3：故事编辑 + 视频生成页]** — 故事模板选择 + 配音预览 + 视频合成 6 步进度可视化 + 最终视频播放

---

## 二、Demo 创作思路

### 灵感来源

去年春节回家，翻到奶奶 18 岁时的老照片——模糊、泛黄、还有折痕。奶奶看着照片讲了一个小时的故事：那年她刚考上师范学校，每天天不亮就起床读书，晚上在煤油灯下备课。但照片本身无法承载这些记忆，故事只能口口相传。

我试了 Remini 修复照片，效果不错，但修完就结束了——照片背后的故事无法被记录，照片本身也不会"动"。如果能把「修复 + 故事 + 动态」整合成一段完整的回忆短片，那该多好。

### 想解决的问题

每个家庭都有泛黄、模糊、破损的老照片。现有工具存在三个痛点：

- **修图工具止步于"修"** — Remini 等工具修完就结束了，缺乏叙事和动态体验
- **价格昂贵** — Remini ¥68/周，普通家庭难以持续使用
- **记忆无法传承** — 照片背后的故事只能口口相传，没有工具能将「修复 + 故事 + 动态」整合

### 为什么做这个方向

竞品是"修图工具"，我想做的是"记忆活化器"——修图只是手段，故事和情感才是核心。这个方向有三个判断依据：

1. **情感刚需** — 家庭记忆传承是跨代际的刚需，春节、中秋等团聚时刻尤其强烈
2. **技术可行** — 图像修复、TTS、视频合成技术都已成熟，组合起来能产出完整体验
3. **传播潜力** — "我把奶奶 18 岁的照片修好了，她哭了"这类内容天然适合短视频传播

---

## 三、Demo 体验地址

> 📌 **[此处填写体验地址]**
>
> 方式一：部署到公网的体验链接（推荐）
>
> 方式二：将项目打包为 ZIP 上传社区（包含前端 dist + 后端代码 + start.bat 一键启动）

**本地体验方式：**

```bash
# 双击 start.bat 一键启动（自动检查环境、安装依赖、启动前后端）
# 或手动启动：
cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
cd frontend && npx vite --host
```

启动后访问 http://localhost:5173 即可体验完整流程：上传照片 → AI 修复 → 讲述故事 → 生成短片。

---

## 四、TRAE 实践过程

整个 Demo 从创意到可运行，全部在 TRAE 中完成。以下是完整的开发过程。

### 阶段一：创意提案生成

用 TRAE Work 的 Auto 模式，输入一句话需求："做一个 AI 老照片修复网站，上传老照片后自动修复上色，还能讲故事生成视频"。TRAE 自动生成了创意提案 HTML 页面，包含产品定位、用户流程、竞品分析、技术方案和开发计划。

> 📌 **[此处插入截图：TRAE Work 生成创意提案的对话界面]**

**Session ID：** `📌 [双击 TRAE 对话复制 Session ID]`

### 阶段二：项目搭建与核心功能开发

用 TRAE IDE 搭建前后端项目。TRAE 的 Agent 模式可以自动规划任务：创建 React + Vite 前端、Python FastAPI 后端、配置路由和 API 代理。

开发过程中，我把每个功能模块拆成独立任务交给 TRAE：

- **修复引擎** — 让 TRAE 实现多阶段图像处理管线（降噪 → 划痕修复 → 对比度增强 → 锐化 → 上色 → 调色）
- **TTS 配音** — 接入 Edge TTS，输入故事文本输出中文语音
- **视频合成** — 用 FFmpeg 实现 Ken Burns 动态效果 + 背景音乐混合 + 字幕烧录

> 📌 **[此处插入截图：TRAE IDE 中开发修复引擎的对话界面]**

**Session ID：** `📌 [双击 TRAE 对话复制 Session ID]`

### 阶段三：踩坑与调试（真实开发过程）

这部分是最有价值的——记录了用 TRAE 解决真实问题的过程。

**坑 1：前端样式全部丢失**

项目跑起来后发现页面是白底黑字，所有 Tailwind CSS 样式都没生效。用 TRAE 的浏览器工具检查发现 CSS 变量全部为空（`--color-bg` 返回空字符串）。

根因是 `vite.config.js` 缺少 `@tailwindcss/vite` 插件——Tailwind CSS 4 的 `@theme` 指令必须通过这个插件编译。TRAE 帮我定位到问题并添加了插件配置，重启后深色主题和橙色渐变样式全部恢复。

> 📌 **[此处插入截图：TRAE 浏览器工具检查 CSS 变量的界面]**

**坑 2：视频生成 500 错误**

上传照片、修复、生成配音都正常，但到视频合成这一步一直报 500。用 TRAE 调试发现 FFmpeg 报 `Unrecognized option 'loop'`。

深入排查后发现：系统 PATH 中有两个 FFmpeg——TRAE 自带的精简版（`--disable-everything` 编译，只有 scale 和 fps 两个滤镜）和 winget 安装的完整版（支持 zoompan、amix、subtitles 等）。精简版优先级更高，导致所有复杂滤镜命令失败。

TRAE 帮我写了 `_find_ffmpeg()` 函数，自动搜索完整版 FFmpeg：扫描 winget 安装目录，对每个候选执行 `ffmpeg -filters` 检测是否支持 zoompan，找到后所有命令都使用完整版路径。视频生成恢复正常，输出 1080p H.264 + AAC 视频。

**坑 3：修复后照片比修复前更糊**

这是最棘手的问题。用户反馈"修复后的照片比修复前更糊"。用 TRAE 逐步追踪发现三个根因：

1. **盲目放大** — 代码把 400x300 的小图放大到 1706x1280，细节被稀释，视觉上变糊
2. **划痕误判** — `remove_scratches` 把高噪点图像的噪点误判为划痕，用大面积 inpaint 把整张图糊掉（清晰度从 10824 暴跌到 29）
3. **白平衡过强** — `auto_white_balance` 用 100% 灰度世界校正，导致彩色照片严重偏紫

TRAE 帮我重写了整个修复引擎，核心改为自适应策略：先评估质量再决定处理方式，已经清晰的照片只做色彩增强不锐化不降噪，最后有质量守卫环节对比修复前后清晰度。修复后所有测试图像清晰度均提升 1.4-3.5 倍。

> 📌 **[此处插入截图：TRAE 中调试修复算法的对话界面，展示清晰度对比数据]**

**Session ID：** `📌 [双击 TRAE 对话复制 Session ID]`

### 阶段四：体验优化

核心功能跑通后，用 TRAE 继续优化用户体验：

- **多步骤进度反馈** — AI 修复过程展示 6 个步骤（分析特征 → 降噪 → 划痕修复 → 对比度增强 → 上色 → 优化），视频合成展示 6 个步骤（情感分析 → 背景音乐 → Ken Burns → 调色 → 字幕 → 合成），每步有动画和状态指示
- **作品画廊懒加载** — 用 IntersectionObserver 实现滚动到区域才加载，不自动读取旧数据
- **故事模板** — 提供 4 种场景模板（奶奶的故事、父母结婚照、童年全家福、青春岁月）一键填充
- **错误处理** — 友好错误提示 + 一键重试，区分网络错误、文件格式错误等
- **状态持久化** — JSON 文件存储，重启不丢失数据

> 📌 **[此处插入截图：TRAE 中优化前端体验的对话界面]`

**Session ID：** `📌 [双击 TRAE 对话复制 Session ID]`

### 技术栈

| 层 | 技术 | 用途 |
|---|---|---|
| 前端 | React 19 + Vite 8 + Tailwind CSS 4 | 界面、路由、样式 |
| 后端 | Python 3.11 + FastAPI | API 服务 |
| 图像处理 | OpenCV + Pillow | 降噪、锐化、上色、调色 |
| 语音合成 | Edge TTS | 免费中文 TTS |
| 视频合成 | FFmpeg（完整版） | Ken Burns 效果 + 音乐混合 + 字幕 |

### 项目结构

```
web-old-photo-time-machine/
├── frontend/src/
│   ├── pages/Home.jsx        # 首页（画廊 + 统计动画）
│   ├── pages/Upload.jsx      # 上传 + 修复对比
│   └── pages/Result.jsx      # 故事 + 配音 + 视频合成
├── backend/app/
│   ├── services/restoration.py    # 自适应修复引擎
│   ├── services/tts.py            # Edge TTS 配音
│   ├── services/video_composer.py # Ken Burns 短片合成
│   └── routers/                   # API 路由
├── start.bat                 # 一键启动
└── creative-proposal.html    # 创意提案
```

---

## 五、报名帖链接

> 📌 **[此处填写你的社区报名帖链接]**

---

## 六、开发心得

用 TRAE 开发这个项目，最大的感受是：**AI 不只是写代码的工具，更是调试和解决问题的搭档。**

最典型的例子是"修复后照片变糊"这个问题。如果是一个人开发，可能需要反复试错：调参数、改算法、对比效果。但用 TRAE，我可以直接描述问题（"修复后的照片比修复前更糊"），TRAE 会帮我分析根因（盲目放大、划痕误判、白平衡过强），然后逐步修复并验证效果（用 Laplacian 方差量化清晰度变化）。

另一个感受是 TRAE 的浏览器工具很有用。前端样式丢失的问题，用浏览器工具检查 CSS 变量，一眼就看到 `--color-bg` 返回空字符串，直接定位到 Tailwind 插件缺失的问题。

FFmpeg 精简版的问题也很有意思——TRAE 帮我写了自动检测完整版 FFmpeg 的函数，这种"环境自适应"的解决方案比手动改 PATH 优雅得多。

这个 Demo 不完美，修复算法用的是 OpenCV 传统方法（后续可升级 GFPGAN/DeOldify 深度学习模型），视频效果也比较基础。但它跑通了完整流程：上传 → 修复 → 上色 → 讲故事 → 配音 → 动态短片，每一步都能体验到真实价值。这正是初赛 Demo 的意义——**有过程、有体验、讲得清楚。**
