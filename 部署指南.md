# 部署指南 — AI 老照片时光机

## 推荐方案：Railway 一键部署（免费额度足够）

### 前提条件

- 一个 GitHub 账号
- 项目已推送到 GitHub 仓库

### 步骤

#### 1. 将项目推送到 GitHub

```bash
cd d:\桌面\Workspace\projects\web-old-photo-time-machine
git init
git add .
git commit -m "AI 老照片时光机 - 初赛 Demo"
git remote add origin https://github.com/你的用户名/old-photo-time-machine.git
git push -u origin main
```

#### 2. 在 Railway 部署

1. 打开 https://railway.app ，点击 "Login" 用 GitHub 账号登录
2. 点击 "New Project" → "Deploy from GitHub repo"
3. 选择你刚推送的仓库
4. Railway 会自动检测 Dockerfile 并开始构建
5. 构建完成后，在 "Settings" → "Networking" 点击 "Generate Domain"
6. 获得公网链接，如 `https://old-photo-time-machine.up.railway.app`

#### 3. 验证部署

- 访问 `https://你的域名/api/health` — 应返回 `{"status":"ok","ffmpeg":true}`
- 访问 `https://你的域名` — 应显示前端首页
- 上传一张照片测试完整流程

#### 4. 将链接填入初赛作品帖

把公网链接填入作品帖的"Demo 体验地址"部分。

---

## 备选方案：Render 部署

1. 打开 https://render.com ，用 GitHub 登录
2. "New" → "Web Service" → 选择你的 GitHub 仓库
3. 配置：
   - Environment: Docker
   - Dockerfile Path: `./Dockerfile`
4. 点击 "Create Web Service"
5. 部署完成后获得 `https://你的应用名.onrender.com`

---

## 备选方案：自有 VPS 部署

如果你的云服务器已安装 Docker：

```bash
# 在服务器上克隆仓库
git clone https://github.com/你的用户名/old-photo-time-machine.git
cd old-photo-time-machine

# 构建并运行
docker build -t old-photo-time-machine .
docker run -d -p 80:8000 --name photo-app --restart always old-photo-time-machine

# 访问 http://你的服务器IP
```

---

## Dockerfile 说明

项目根目录的 `Dockerfile` 采用两阶段构建：

| 阶段 | 基础镜像 | 作用 |
|---|---|---|
| Stage 1 | node:20-slim | 安装前端依赖，构建静态文件到 /static |
| Stage 2 | python:3.11-slim | 安装 FFmpeg + Python 依赖，复制后端代码和前端静态文件 |

最终镜像包含：
- Python 3.11 + FastAPI 后端
- FFmpeg 完整版（apt 安装，支持 zoompan/amix/subtitles 等全部滤镜）
- OpenCV + Pillow + Edge TTS
- 前端静态文件（React 构建产物）

启动命令：`uvicorn app.main:app --host 0.0.0.0 --port 8000`

后端自动托管前端静态文件，无需单独部署前端。
